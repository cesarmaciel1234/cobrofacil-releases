# Documentación de la pirámide

La regla está en `modularizacion.md`, en la raíz del programa.

Al mejorar un módulo se ofrece y se aplica: `plano.md` en la carpeta donde arranca la rama, y `README.md` en cada subcarpeta.

Auditoría de inventario: `src/admin/auditoria_inventario/plano.md`.
