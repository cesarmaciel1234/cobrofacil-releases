Issues titled "[ci] token-check (cerrar)" are not production bugs.

Body "Validacion automatica del secret. Se puede cerrar." is created by the
release workflow (.github/workflows/release.yml) to verify that
COBROFACIL_ERROR_REPORT_TOKEN can call the GitHub Issues API. The issue is
closed immediately after creation when the token check succeeds.

No code change or version bump is required. Close these issues as CI noise.
