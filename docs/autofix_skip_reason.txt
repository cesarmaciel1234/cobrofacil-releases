Issues like #27, #30, #33, #35, #40, #45, #46, #51, #54, #60, #65, #72, and #74 are not production bugs.

Title "[ci] token-check (cerrar)" and body "Validacion automatica del secret. Se puede cerrar."
are created by the release workflow (.github/workflows/release.yml) to verify that
COBROFACIL_ERROR_REPORT_TOKEN can call the GitHub Issues API. The issue is closed
immediately after creation when the token check succeeds.

No code change or version bump is required. Close these issues as CI noise.
