# Auditoría de inventario

Las IA leen `plano.md` de esta carpeta antes de modificar el módulo. Acá arranca la rama.

La pantalla está en `auditoria_main.py`. El fondo está documentado en `plano.md` y el detalle del motor en `src/cerebro_global/auditoria/README.md`.

La guía larga de procedimientos sigue en `DOCUMENTACION_AJUSTES_MATERIA_PRIMA.md`. Si esa guía nombra tablas o funciones que no están en `plano.md`, manda el código y el plano.
