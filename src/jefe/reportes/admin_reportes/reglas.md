# admin_reportes/ — legado

Pantalla vieja de admin (`Admin3Reportes`).  
El jefe **no** entra por acá. La puerta es `../reportes_main.py`.

Si hay que arreglar un número o un período, hacelo en `financiero/` y `periodo/`.  
No copies lógica nueva en estos componentes.
