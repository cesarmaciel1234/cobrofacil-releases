# Botón del asistente

`boton.py` define `BotonChatbot`. Solo pinta el botón de la barra. El chat está en `componentes_barra_inferior/chatbot`.

Si este paquete no viaja en el ejecutable, la venta igual abre: la barra lo salta.
