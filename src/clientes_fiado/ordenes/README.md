# Órdenes

Estas piezas reexportan al garante, para no romper el camino viejo.

`orden.py` reexporta `OrdenCobro`, que vive en `garante/orden/orden.py`. `despacho.py` reexporta `entregar`, que vive en `garante/despacho/despacho.py`.
